# Live trading runtime (Windows VPS + MT5 terminal). Install inside a venv:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements_live.txt
MetaTrader5>=5.0.45     # broker API — Windows only, needs the MT5 terminal installed
pandas>=2.0
numpy>=1.24
pytz>=2023.3            # timezone handling (used by news_filter + backtest signal logic)
plotly>=5.0             # chart_trades.py HTML trade viewer
certifi>=2024.0         # up-to-date CA bundle for Telegram HTTPS on Windows VPS
matplotlib>=3.7         # equity curve chart for report_telegram.py
